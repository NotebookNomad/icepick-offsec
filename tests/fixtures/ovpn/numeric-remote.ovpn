# Synthetic fixture - NOT a working config, no real key material.
# The remote is a literal address from TEST-NET-3 (RFC 5737, never routed) so
# lockdown-wan's endpoint rule can be asserted without depending on DNS.
client
dev tun
proto udp
remote 203.0.113.77 1194
resolv-retry infinite
nobind
persist-tun
